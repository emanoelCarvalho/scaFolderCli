# Architecture Decision Records

One file per decision that would be expensive to reverse. Format: context, the
decision, consequences, and the alternatives that were rejected and why.

Add an ADR when a choice constrains future work. Do not add one for a choice
that a single commit could undo.
