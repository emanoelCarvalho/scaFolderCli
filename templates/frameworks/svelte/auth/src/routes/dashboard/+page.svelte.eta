<script lang="ts">
	import Button from '$lib/components/Button.svelte';

	let { data } = $props();
</script>

<svelte:head><title>Dashboard</title></svelte:head>

<main class="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-6 px-6">
	<div>
		<h1 class="text-2xl font-semibold">Signed in</h1>
		<p class="mt-1 text-sm opacity-70">{data.user.email}</p>
	</div>
	<p class="text-sm opacity-70">
		This page is rendered on the server and reads the session from an httpOnly cookie. The access
		token never reaches the browser.
	</p>
	<form method="POST" action="/logout">
		<Button type="submit" variant="secondary">Sign out</Button>
	</form>
</main>
